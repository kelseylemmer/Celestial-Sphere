import { ProfileForm } from "./CreateProfileForm.js";


export const Profile = () => {
    return (
        <div>
            <ProfileForm />
        </div>
    );
};

//    const navigate = useNavigate();
// const handleButtonClick = () => {
//     navigate("/CreateProfile");
// };

//<Button onClick={handleButtonClick} text="Create Profile" />
